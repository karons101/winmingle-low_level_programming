# 0x12. C - Singly linked lists

## Description
This project covers the implementation and usage of singly linked lists in C. It includes functions for printing lists, calculating length, adding nodes at the beginning and end, and freeing memory.

## Files
| File | Description |
| --- | --- |
| `lists.h` | Header file containing the `list_t` struct and function prototypes. |
| `0-print_list.c` | Prints all elements of a `list_t` list. |
| `1-list_len.c` | Returns the number of elements in a `list_t` list. |
| `2-add_node.c` | Adds a new node at the beginning of the list. |
| `3-add_node_end.c` | Adds a new node at the end of the list. |
| `4-free_list.c` | Frees a `list_t` list. |
| `100-first.c` | Prints a message before the `main` function is executed. |
| `101-hello_winmingle.asm` | 64-bit assembly program that prints "Hello, WINMINGLE". |
