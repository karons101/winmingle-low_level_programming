#include <stdio.h>

/**
 * Steps taken in this multi line comments:
   Here is the  main - Entry point
 *
 * Description: Prints the string "with proper grammar, but the outcome is a piece of art,"
 * followed by a new line using printf().
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    /* printf requires the explicit newline character \n */
    printf("with proper grammar, but the outcome is a piece of art,\n");
    
    return (0);
}
