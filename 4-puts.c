#include <stdio.h>

int main(void)
{
    /*  I found out The puts function prints the string and appends a newline character */
    puts("\"Programming is like building a multilingual puzzle");
    
    return (0);
}
